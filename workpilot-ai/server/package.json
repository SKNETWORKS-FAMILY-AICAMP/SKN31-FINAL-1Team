{
  "name": "workpilot-ai-server",
  "version": "0.1.0",
  "private": true,
  "description": "WorkPilot AI 백엔드 — 네트워크 호출은 Node.js 내장 fetch만 사용한다. 회의 요약 첨부파일(pdf/docx)의 텍스트 추출을 위해 pdf-parse/mammoth만 예외적으로 의존한다.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "pdf-parse": "^1.1.1",
    "mammoth": "^1.8.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^20.14.10"
  }
}
