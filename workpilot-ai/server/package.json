{
  "name": "workpilot-ai-server",
  "version": "0.1.0",
  "private": true,
  "description": "WorkPilot AI 백엔드 — 외부 런타임 의존성 없이 Node.js 내장 http 모듈만 사용한다.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^20.14.10"
  }
}
